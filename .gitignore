# ── secrets — never commit ────────────────────────────────────────────────
.env
.env.*
*.secret
secrets/
CONTENT_INGEST_SECRET*

# ── render output & scratch ───────────────────────────────────────────────
out_*/
_filled.html
fonts/

# ── send log: contains card ids and paths, regenerate locally ─────────────
ingest_log.jsonl

# ── python ────────────────────────────────────────────────────────────────
__pycache__/
*.pyc
.venv/
venv/

# ── os noise ──────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── supplier-confidential: certificates & manufacturer documents ──────────
# These identify the manufacturer. They live in a private drive, never in git.
CERTS/
certs/
